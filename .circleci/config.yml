version: 2.1

orbs:
  node: circleci/node@5.1.0

jobs:
  build_and_test:
    docker:
      - image: cimg/node:18.13
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          name: Run lint
          command: yarn lint
      - run:
          name: Run tests
          command: yarn test
      - run:
          name: Run build
          command: yarn build

workflows:
  build_and_test:
    jobs:
      - build_and_test
